#!/data/data/com.termux/files/usr/bin/bash
rm -f setup.log

apt-get update &>> setup.log && apt-get upgrade &>> setup.log
apt-get install termux-api &>> setup.log

sed -i '2 a termux-fingerprint | grep -q "AUTH_RESULT_SUCCESS"; [ $? -ne 0 ] && exit' ~/../usr/bin/login
